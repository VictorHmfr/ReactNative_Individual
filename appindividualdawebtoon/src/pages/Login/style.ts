import { StyleSheet } from "react-native";

export const webtoon = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#2E2F33',
    alignItems: 'center',
    justifyContent: 'center',
    gap: 20,
  },

  title: {
    fontSize: 30,
    textAlign: 'center',

  },
});